{
  "$schema": "../../schemas/integration-v1.json",
  "schema": "ipad-agent.integration/v1",
  "version": 1,
  "id": "settings",
  "name": "Settings",
  "kind": "core",
  "bundle_ids": [
    "com.apple.Preferences"
  ],
  "aliases": [
    "settings",
    "apple settings"
  ],
  "capabilities": [
    "exact-settings-url-navigation"
  ],
  "selectors": {},
  "actions": {},
  "scenarios": {},
  "safety": {
    "classification": "settings-navigation-only",
    "mutates_user_data": false,
    "requires_confirmation": [],
    "prohibited": [
      "arbitrary URLs, caller parameters, retries, or caller-supplied dynamic identifiers",
      "dispatching candidate, incompatible, template, or blocked route-catalogue entries through production commands",
      "operating a toggle, row control, picker, text field, button, network, device, app, account, or protected confirmation",
      "payment, reset, erase, install, update, profile, passcode, biometric, account-changing, or query-action targets"
    ],
    "uncertain_outcome": "CoreDevice acceptance proves dispatch only, not rendered visibility. A lost response must not be retried or converted into compatibility evidence."
  },
  "retry": {
    "default": "inspect_then_decide",
    "max_attempts": 1,
    "idempotent_actions": []
  },
  "requirements": {
    "host": [
      "macOS with Xcode CoreDevice support",
      "Python 3.11 or newer"
    ],
    "device": [
      "paired physical iPad",
      "unlocked iPad"
    ],
    "permissions": [
      "Trust This Computer"
    ]
  },
  "compatibility": {
    "platform": "iPadOS",
    "minimum_os": "26.6.1",
    "maximum_os": "26.6.1",
    "locales": [
      "not-applicable"
    ],
    "verification": "unverified",
    "app_versions": []
  },
  "privacy": {
    "data_access": [],
    "data_sent": [],
    "retention": "The integration stores no Settings values, network names, device names, account data, or identifiers.",
    "notes": "The catalogue retains 248 exact runtime literals from the separate iOS 26.5 simulator (23F77) and structured user evidence for the target model/build profile. Ordinary deep links are limited to 11 user-visual passes bound to product iPad17,1, hardware J817AP, and OS build 23G83. The remaining concrete routes are development-only candidates or incompatible; dynamic routes are inert templates. Apple does not support the private scheme, and the schema-required integration-wide verification field remains unverified."
  }
}
